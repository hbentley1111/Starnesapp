DATABASE_URL=postgres://starnes:starnes@localhost:5432/starnes
REDIS_URL=redis://localhost:6379
# Google OAuth (client-owned Google Cloud project) — never commit real values
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_ALLOWED_HD=starnesrealestate.com
# Anthropic (server-side only)
ANTHROPIC_API_KEY=
# Webhook shared secrets (vault-managed in cloud envs)
FIREFLIES_WEBHOOK_SECRET=
LEAD_FORM_SHARED_SECRET=
# HITL dispatch-token signing key
HITL_TOKEN_SIGNING_KEY=
# RentCast (property search + recent commercial sales). Server-side only.
# Without a key the API serves clearly-labeled sample data.
RENTCAST_API_KEY=
RENTCAST_MARKET_CITY=Salisbury
RENTCAST_MARKET_STATE=NC
